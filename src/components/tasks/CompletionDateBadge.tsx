const CompletionDateBadge = () => {
    return (
        <div
            className={
                "rounded-full mr-[5px] px-[5px] text-sm bg-main h-[20px]"
            }
        >
            Completion Date
        </div>
    );
};

export default CompletionDateBadge;
